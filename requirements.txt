# ═══════════════════════════════════════════════════════════════
# REQUIREMENTS.TXT — Every library this project needs
# ═══════════════════════════════════════════════════════════════
# Install with: pip install -r requirements.txt
#
# WHY EACH LIBRARY:
#   pandas      — data manipulation (DataFrames, CSV reading)
#   numpy       — numerical operations (arrays, math)
#   pydantic    — data validation (type checking, schema enforcement)
#   scikit-learn — ML utilities (train/test split, metrics)
#   xgboost     — gradient boosted trees (demand forecasting)
#   statsmodels — ARIMA time-series model
#   mlflow      — experiment tracking (model versioning)
#   langchain   — LLM orchestration (RAG pipeline)
#   chromadb    — vector database (semantic search)
#   openai      — GPT-4 API client
#   sentence-transformers — text embeddings
#   fastapi     — REST API framework
#   uvicorn     — ASGI server (runs FastAPI)
#   streamlit   — dashboard UI
#   pytest      — testing framework
#   matplotlib  — plotting / visualization
#   seaborn     — statistical visualization

# ── Phase 1: Data Ingestion ──
pandas>=2.0.0
numpy>=1.24.0
pydantic>=2.0.0

# ── Phase 2: ML Forecasting ──
scikit-learn>=1.3.0
xgboost>=2.0.0
statsmodels>=0.14.0
mlflow>=2.8.0
matplotlib>=3.7.0
seaborn>=0.12.0

# ── Phase 3: GenAI / RAG ──
langchain>=0.1.0
chromadb>=0.4.0
openai>=1.0.0
sentence-transformers>=2.2.0

# ── Phase 4: Deployment ──
fastapi>=0.104.0
uvicorn>=0.24.0
streamlit>=1.28.0
python-dotenv>=1.0.0

# ── Testing ──
pytest>=7.4.0
